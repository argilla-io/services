# Services

This repo contains demo services related to NLP tasks and knowledge graphs.

## Information extraction demo REST

This REST service is based on the demo service of spaCy but adds several methods to perform triple and information extraction.

<!-- To run (and build) nlp service. Placed in nlp folder, type the command

``docker-compose up -d``

To rebuild

``docker-compose up --build -d` -->
